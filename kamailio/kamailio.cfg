#!KAMAILIO
#
# PBX Cloud - Multi-Tenant SIP Proxy Configuration
# Kamailio 5.7 - Edge proxy with tenant routing
#

####### Global Parameters #########

debug=2
log_stderror=yes
log_facility=LOG_LOCAL0

memdbg=5
memlog=5

children=4
auto_aliases=no

# Listen on UDP port 5060 (edge SIP port)
listen=udp:0.0.0.0:5060 advertise ${EXTERNAL_IP}:5060

# Asterisk backend address (substituted by entrypoint)
#!substdef "!ASTERISK_IP!${ASTERISK_IP}!g"
#!substdef "!ASTERISK_PORT!${ASTERISK_PORT}!g"

####### Modules Section ########

loadmodule "kex.so"
loadmodule "corex.so"
loadmodule "tm.so"
loadmodule "tmx.so"
loadmodule "sl.so"
loadmodule "rr.so"
loadmodule "pv.so"
loadmodule "maxfwd.so"
loadmodule "textops.so"
loadmodule "siputils.so"
loadmodule "xlog.so"
loadmodule "sanity.so"
loadmodule "usrloc.so"
loadmodule "nathelper.so"
loadmodule "htable.so"
loadmodule "path.so"
loadmodule "pike.so"
loadmodule "textopsx.so"

# ---- Module Parameters ----

# Transaction module
modparam("tm", "failure_reply_mode", 3)
modparam("tm", "fr_timer", 30000)
modparam("tm", "fr_inv_timer", 120000)

# Record-Route
modparam("rr", "enable_full_lr", 1)
modparam("rr", "append_fromtag", 1)

# NAT helper — Kamailio is stateless proxy, no NAT keepalive needed
# (Asterisk handles registrations and keepalive via qualify_frequency)
modparam("nathelper", "received_avp", "$avp(received)")

# usrloc — memory-only mode (Asterisk handles registrations, not Kamailio)
modparam("usrloc", "db_mode", 0)

# Pike — basic flood protection (Phase 4 will enhance)
modparam("pike", "sampling_time_unit", 2)
modparam("pike", "reqs_density_per_unit", 30)
modparam("pike", "remove_latency", 4)

# htable — tenant domain lookup table
# Format: key = SIP domain, value = tenant slug
modparam("htable", "htable", "tenants=>size=4;initval=0;")

####### Routing Logic ########

# Main request route
request_route {

    # ---- Per-request initial checks ----
    if (!mf_process_maxfwd_header("10")) {
        sl_send_reply("483", "Too Many Hops");
        exit;
    }

    # Sanity check
    if (!sanity_check("17895", "7")) {
        xlog("L_WARN", "Malformed SIP message from $si:$sp\n");
        exit;
    }

    # CANCEL processing
    if (is_method("CANCEL")) {
        if (t_check_trans()) {
            t_relay();
        }
        exit;
    }

    # Handle retransmissions
    if (!is_method("ACK")) {
        if (t_precheck_trans()) {
            t_check_trans();
            exit;
        }
        t_check_trans();
    }

    # ---- Anti-loop: detect requests from Asterisk ----
    # If the request comes from Asterisk (port ASTERISK_PORT), let it through
    # without re-processing tenant logic. Asterisk B2BUA sends NEW INVITEs
    # with Route headers (from Path) — loose_route() processes them.
    if ($sp == ASTERISK_PORT && $si == "ASTERISK_IP") {
        xlog("L_INFO", "Request from Asterisk [$rm $ru] — forwarding outbound\n");
        if (is_method("INVITE|SUBSCRIBE")) {
            record_route();
        }
        # Process Route headers (from Path-based routing)
        if (loose_route()) {
            xlog("L_INFO", "Asterisk outbound via loose_route to $du\n");
            route(RELAY);
            exit;
        }
        route(RELAY);
        exit;
    }

    # ---- NAT detection and fixing ----
    if (nat_uac_test("19")) {
        fix_nated_contact();
        force_rport();
    }

    # ---- Sequential (in-dialog) requests ----
    if (has_totag()) {
        if (loose_route()) {
            if (is_method("BYE")) {
                xlog("L_INFO", "BYE in-dialog: $fu -> $tu\n");
            }
            route(RELAY);
            exit;
        } else {
            if (is_method("ACK")) {
                if (t_check_trans()) {
                    route(RELAY);
                    exit;
                } else {
                    exit;
                }
            }
            sl_send_reply("404", "Not Found");
            exit;
        }
    }

    # ---- Initial requests ----

    # Record-Route for INVITE and SUBSCRIBE
    if (is_method("INVITE|SUBSCRIBE")) {
        record_route();
    }

    # ---- REGISTER handling ----
    if (is_method("REGISTER")) {
        route(TENANT_LOOKUP);
        if ($avp(tenant_id) == $null) {
            xlog("L_WARN", "REGISTER rejected: unknown domain [$td]\n");
            sl_send_reply("403", "Unknown Tenant Domain");
            exit;
        }
        xlog("L_INFO", "REGISTER from [$fu] tenant=[$avp(tenant_id)] domain=[$td]\n");

        # Add Path header so Asterisk routes outbound calls through Kamailio
        # (fix_nated_contact already preserves client's real IP in Contact)
        add_path();

        # Add tenant header and forward to Asterisk
        remove_hf("X-Tenant-ID");
        append_hf("X-Tenant-ID: $avp(tenant_id)\r\n");

        # Forward to Asterisk
        route(TO_ASTERISK);
        exit;
    }

    # ---- INVITE and other initial requests ----
    if (is_method("INVITE")) {
        route(TENANT_LOOKUP);
        if ($avp(tenant_id) == $null) {
            xlog("L_WARN", "INVITE rejected: unknown domain [$rd]\n");
            sl_send_reply("404", "Unknown Tenant Domain");
            exit;
        }
        xlog("L_INFO", "INVITE [$fu] -> [$ru] tenant=[$avp(tenant_id)]\n");

        # Add tenant header and forward to Asterisk
        remove_hf("X-Tenant-ID");
        append_hf("X-Tenant-ID: $avp(tenant_id)\r\n");

        route(TO_ASTERISK);
        exit;
    }

    # ---- Other methods (OPTIONS, etc.) ----
    if (is_method("OPTIONS")) {
        # Respond to OPTIONS pings (keepalive)
        sl_send_reply("200", "OK");
        exit;
    }

    # Default: forward to Asterisk
    route(TENANT_LOOKUP);
    if ($avp(tenant_id) != $null) {
        remove_hf("X-Tenant-ID");
        append_hf("X-Tenant-ID: $avp(tenant_id)\r\n");
    }
    route(TO_ASTERISK);
    exit;
}

# ---- Tenant domain lookup ----
route[TENANT_LOOKUP] {
    # Extract domain from To-URI for REGISTER, Request-URI for others
    $var(lookup_domain) = $rd;
    if (is_method("REGISTER")) {
        $var(lookup_domain) = $td;
    }

    xlog("L_DBG", "Tenant lookup for domain: [$var(lookup_domain)]\n");

    # Lookup in htable
    $avp(tenant_id) = $sht(tenants=>$var(lookup_domain));
    if ($avp(tenant_id) == 0 || $avp(tenant_id) == $null) {
        $avp(tenant_id) = $null;
        xlog("L_WARN", "No tenant found for domain: [$var(lookup_domain)]\n");
    } else {
        xlog("L_INFO", "Resolved domain [$var(lookup_domain)] -> tenant [$avp(tenant_id)]\n");
    }
}

# ---- Forward to Asterisk ----
route[TO_ASTERISK] {
    $du = "sip:ASTERISK_IP:ASTERISK_PORT";
    xlog("L_INFO", "Forwarding to Asterisk: $du [$rm $ru]\n");
    route(RELAY);
}

# ---- Generic relay ----
route[RELAY] {
    if (!t_relay()) {
        sl_reply_error();
    }
    exit;
}

# ---- Reply route ----
onreply_route {
    if (nat_uac_test("1")) {
        fix_nated_contact();
    }
}

# ---- Failure route ----
failure_route[FAIL_ASTERISK] {
    if (t_is_canceled()) {
        exit;
    }
    xlog("L_ERR", "Asterisk failure for [$rm $ru]: $T_reply_code $T_reply_reason\n");
}

# ---- Event route: startup — populate htable ----
event_route[htable:mod-init] {
    # Populate tenant domains
    # Add new tenants here (or reload via kamctl)
    $sht(tenants=>acme.pbx.local) = "acme";
    $sht(tenants=>globex.pbx.local) = "globex";

    xlog("L_INFO", "Tenant htable initialized: acme.pbx.local, globex.pbx.local\n");
}
