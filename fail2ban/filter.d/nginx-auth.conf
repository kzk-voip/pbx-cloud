# fail2ban filter for Nginx API authentication failures
# Matches HTTP 401/429 responses on auth endpoints

[Definition]
failregex = ^<HOST> .* "POST /auth/login HTTP/.*" (401|429)
            ^<HOST> .* "POST /auth/.* HTTP/.*" (401|429)

ignoreregex =
