# Database
POSTGRES_DB=pbx
POSTGRES_USER=pbx_user
POSTGRES_PASSWORD=changeme

# Redis (for future use)
REDIS_PASSWORD=changeme

# API (for future use)
JWT_SECRET=changeme
JWT_ALGORITHM=HS256

# Asterisk AMI (for future use)
AMI_USERNAME=admin
AMI_PASSWORD=changeme

# Network
EXTERNAL_IP=100.88.43.14
SIP_DOMAIN=pbx.local