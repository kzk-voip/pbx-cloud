# Database
POSTGRES_DB=pbx
POSTGRES_USER=pbx_user
POSTGRES_PASSWORD=changeme

# Network
EXTERNAL_IP=100.88.43.14

# Redis (Phase 3+)
REDIS_PASSWORD=changeme

# API (Phase 3+)
JWT_SECRET=changeme
JWT_ALGORITHM=HS256

# Asterisk AMI (Phase 3+)
AMI_USERNAME=admin
AMI_PASSWORD=changeme

# Network (Tailscale or public IP of the server)
EXTERNAL_IP=changeme