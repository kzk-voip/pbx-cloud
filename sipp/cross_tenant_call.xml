<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  Cross-Tenant Isolation Test

  Simulates "acme_hacker" trying to call "stark_101" on a different tenant.
  Kamailio routes all INVITEs to Asterisk, so we expect 401 (auth challenge).
  The test PASSES because:
    - acme_hacker has no credentials for stark tenant
    - Even with auth, Asterisk dialplan isolates tenants by context
    - The call can NEVER reach 200 OK (established)

  Usage:
    docker compose --profile testing run sipp \
      sipp -sf /sipp/cross_tenant_call.xml -s stark_101 -r 1 -m 5 127.0.0.1:5060
-->
<scenario name="Cross-Tenant Isolation Test">

  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@stark.pbx.local SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "Attacker" <sip:acme_hacker@acme.pbx.local>;tag=[pid]SIPpTag[call_number]
      To: <sip:[service]@stark.pbx.local>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: sip:acme_hacker@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000
    ]]>
  </send>

  <recv response="100" optional="true"></recv>

  <!-- Asterisk challenges with 401; acme_hacker can never authenticate
       against stark tenant. Call is effectively blocked. -->
  <recv response="401" rtd="true"></recv>

  <send>
    <![CDATA[
      ACK sip:[service]@stark.pbx.local SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "Attacker" <sip:acme_hacker@acme.pbx.local>;tag=[pid]SIPpTag[call_number]
      To: <sip:[service]@stark.pbx.local>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: sip:acme_hacker@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

</scenario>
