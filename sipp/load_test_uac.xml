<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  UAC Load Test — generates concurrent INVITEs for stress testing.
  
  Each call sends INVITE, gets 401 (auth challenge), ACKs it.
  This tests Kamailio routing + Asterisk responsiveness under load.

  Usage (50 concurrent, 10 CPS):
    docker compose - -profile testing run sipp \
      sipp -sf /sipp/load_test_uac.xml \
      -s acme_101 -r 10 -l 50 -m 200 \
      127.0.0.1:5060

  Monitor:
    docker compose exec asterisk-1 asterisk -rx "core show channels count"
    docker compose logs kamailio --tail 20 | grep "Dispatching"
-->
<scenario name="UAC Load Test">

  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@acme.pbx.local SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "Load UAC" <sip:acme_[call_number]@acme.pbx.local>;tag=[pid]SIPpTag[call_number]
      To: <sip:[service]@acme.pbx.local>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: sip:acme_[call_number]@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000
    ]]>
  </send>

  <recv response="100" optional="true"></recv>

  <!-- Asterisk will challenge — 401 is mandatory -->
  <recv response="401" rtd="true"></recv>

  <send>
    <![CDATA[
      ACK sip:[service]@acme.pbx.local SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "Load UAC" <sip:acme_[call_number]@acme.pbx.local>;tag=[pid]SIPpTag[call_number]
      To: <sip:[service]@acme.pbx.local>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: sip:acme_[call_number]@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

</scenario>
