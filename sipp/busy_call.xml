<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  Busy Call Test — INVITE to an extension that should return 486/401.
  
  In practice, without a second active call on the target extension,
  Asterisk will challenge with 401. Both 401 and 486 are valid results.

  Usage:
    docker compose - -profile testing run sipp \
      sipp -sf /sipp/busy_call.xml -s acme_101 -r 1 -l 1 -m 5 127.0.0.1:5060
-->
<scenario name="Busy Call Test">

  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@acme.pbx.local SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "SIPp Busy" <sip:acme_test@acme.pbx.local>;tag=[pid]SIPpTag[call_number]
      To: <sip:[service]@acme.pbx.local>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: sip:acme_test@[local_ip]:[local_port]
      Max-Forwards: 70
      Subject: Busy Call
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000
    ]]>
  </send>

  <recv response="100" optional="true"></recv>

  <!-- Expect 401 (auth challenge) or 486 (busy) — 401 is mandatory fallback -->
  <recv response="486" optional="true" rtd="true"></recv>
  <recv response="401" rtd="true"></recv>

  <send>
    <![CDATA[
      ACK sip:[service]@acme.pbx.local SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "SIPp Busy" <sip:acme_test@acme.pbx.local>;tag=[pid]SIPpTag[call_number]
      To: <sip:[service]@acme.pbx.local>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: sip:acme_test@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

</scenario>
