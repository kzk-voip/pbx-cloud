; ==========================================================
; PJSIP Configuration — Multi-tenant PBX
; ==========================================================
; All endpoint/auth/aor definitions are in PostgreSQL (ARA).
; Only transport and template definitions are here.

[transport-udp]
type=transport
protocol=udp
bind=0.0.0.0:5060