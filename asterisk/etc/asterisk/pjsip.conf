[transport-udp]
type=transport
protocol=udp
bind=0.0.0.0:5080

; --- Default endpoint template for tenant users ---
; All actual endpoints are in PostgreSQL (ARA) — not in this file.
; The template is kept here as documentation of default values.
; New endpoints are created via INSERT into ps_endpoints.

; NOTE: Hardcoded endpoints removed — all endpoints managed via DB.
; See asterisk/sql/02_tenant_schema.sql for seed data.